<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bandname generator</title>
    <link rel="stylesheet" href="/styles/style.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
</head>
<body class="w-auto h-auto bg-dark">
    <main class="d-flex flex-column align-items-center justify-content-center p-3 mb-2 text-white ">
        <div class="band-content"> 
            <div id="band-name">
                <%# upon home page load, check if band name and emoji are set and then display %>
                <% if (locals.bandName) { %>
                    <h2><%= bandName%>
                        <% if (locals.bandEmoji) { %>
                            <span><%= bandEmoji %></span>
                        <% } %>
                    </h2> 
                <% } else { %>
                    <h2>Welcome to Band Name Generator</h2>
                <% }  %> 
            </div>
            <form action="/generate" method="post"> 
                <button type="submit" class="btn mr-md-2 mb-md-0 mb-2 mt-3 btn-light">Generate Name</button>
            </form>
        </div>
        <div id="footer">
            <span id="copyright">Copyright &copy; <%= new Date().getFullYear() %></span>
        </div>
    </main>
</body>
</html>